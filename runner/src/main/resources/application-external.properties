broker.artemis.url=tcp://localhost:61616
broker.pulsar.url=pulsar://localhost:6650
broker.artemis.durability=Artemis standalone (Docker) — durabilité selon config broker
broker.pulsar.durability=Pulsar standalone (Docker) — BookKeeper, durabilité selon config broker
